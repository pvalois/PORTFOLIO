## Role to install apache web server

This role provides : 

* parameters to make installation customizable (see *defaults/main.yml*, and *host_vars*)
* templates for default web page
* distribution automatic adjustments (package name, files locations, ...)


