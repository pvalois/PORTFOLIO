upstream fastcgi_backend {
  server  unix:/run/php/php-fpm.sock;
}

server {

  listen 80;
  server_name {{ magento_server_hostname }};
  set $MAGE_ROOT {{ magento_dir }};
  include {{ magento_dir }}/nginx.conf.sample;
}
